# 5. Команди та совіти

## Винахід скорочень

### Проблема: Bash mode без терміналу

Я дивився як використовувати bash mode без відкриття нового терміналу...

> Тогда я подивився на скорочення: `Shift + ?`

### Відкриття скорочень: `!`

```
Замість: Відкрити новий терміналу → запустити команду → закрити
         
Краще: Просто напиши ! у Claude Code
```

**Приклад:**
```
You: "!ls -la"
Claude Code: [runs command, shows output]
You: "!npm test"
Claude Code: [runs test]
```

![Скорочення в Claude Code](./screenshots/shortcuts.webp)

### Обмеження

⚠️ Не може запустити **interactive Python shell** як в звичайному терміналі

```
❌ ! python3 -i  (interactive mode - не працює)
✅ ! python3 script.py  (скрипт - працює)
```

### Люблю кольори!

Claude Code має **красиві кольори** для command output! 🌈

---

## Headless Mode

Claude Code - це **proper CLI tool**, тому можна запустити без інтерактивного UI:

```bash
claude -p "Search the internet and tell me about Anthropic"
```

Це зручно для:
- 🔧 Automation скриптів
- 🤖 Integration з іншими інструментами
- 📋 Батник завдань

---

## @ для посилання на файли

### Я не знав про це 3-4 дні! 😅

Ви можете посилатися на файли используючи **@ символ**:

```
@mentions для файлів

You: "Read @src/auth.py and explain"
Claude: [автоматично читає файл]

Або вказати шлях:
You: "@./config/settings.json what's the timeout value?"
```

### Як знайти це?

Я знайшов це коли подивився на скорочення (**Shift + ?**). Josh9817 мені його підказав!

### Переваги

✅ Швидший доступ до файлів  
✅ Очевидна комунікація - показує які файли ви маєте на увазі  
✅ Уникає копіювання вмісту файлу

---

## Memory - Глобальні інструкції

### Що таке Memory?

[Документація](https://docs.anthropic.com/en/docs/claude-code/memory)

Memory - це як додавання **custom instructions** до системного промпту.

**Користуються** через всі сесії!

### Приклад Memory

```
[Ви налаштовуєте в Settings]

Memory:
"I prefer:
- Concise explanations
- Python over JavaScript  
- Functional programming style
- TDD approach"
```

Потім будь-який чат буде **враховувати ці преференції**.

### CLAUDE.md vs Memory

```
CLAUDE.md:
- Project-specific
- Читається у поточній папці рекурсивно
- Приватна для проекту

Memory:
- Глобальна
- Через всі проекти
- Персональні преференції
```

### Як Claude знаходить Memory

Claude читає **рекурсивно**:

```
/project/foo/bar/
            ↑
      Шукаю CLAUDE.md тут
           ↑
      Шукаю в /project/foo/
           ↑
      Шукаю в /project/
           
NOT:
❌ / (root директорія)
```

**Цікавість**: Claude також знаходить CLAUDE.md в **піддеревах**:

```
/project/
  ├─ CLAUDE.md (глобальна)
  └─ /auth/
      └─ CLAUDE.md (для auth модуля)
```

Коли Claude читає файли в `/auth/`, він завантажує обидва! 📚

---

## Built-in Команди

### Вбудовані команди які я використовував

```
/pr-comments  - Отримати коментарі з PR (потребує GitHub CLI)
/review       - Огляд diff коду
```

Обидві потребують **GitHub CLI** встановленої.

### Як їх використовувати

**Приклад 1: Новий чат для ревю**

```
// Команди 1: Отримати diff
You: "check the diff from main"

// Команда 2: Отримати PR коментарі  
You: "/pr-comments"

// Потім перемкнутися на нову сесію з цим контекстом
```

**Приклад 2: Робочий процес**

```
1. Я роблю зміни на гілці
2. Запускаю /review або check diff from main
3. Claude бачить зміни
4. Claude дає огляд
5. Я кажу: "Start new session"
6. Новий чат із контекстом дифу
```

---

##權Permissions та Skip-Permissions

### Permission Mode

Перед сесією можна налаштувати дозволи:

```
/ permissions
```

Потім Claude запитає якими інструментами вона може користуватися:
- Read files? ✅
- Edit files? ⚠️
- Run commands? ⚠️
- Delete files? ❌ (я завжди вимикаю)

### Dangerously Skip Permissions

Якщо ви **впевнені** що довіряєте Claude:

```bash
claude --dangerously-skip-permissions
```

⚠️ **Використовуйте обережно!**

---

## Custom Commands

### Огляд

Вбудовані команди:
- `/pr-comments` - PR коментарі
- `/review` - код огляд
- та інші...

Але ви можете робити **свої команди**!

### Приклад Custom Command

Думаю про команду, як:

```
/my-custom-command [args]
```

Яка запускала б якусь дію або промпт.

### Як робити

> Я ще не спробував, але це можливо! Деталі в next article.

---

## Hidden Features - Esc twice

### Форкування розмови

Натисніть **Esc двічі швидко**:

```
Esc + Esc (быстро)
```

Це дозволяє вам **форкувати розмову із будь-якої точки**!

**Кейс використання:**

```
Я був на ітерації 10
Хочу повернутись до ітерації 5 и дослідити інший шлях

Esc + Esc → вибираю ітерацію 5
        → нова гілка починається звідти
```

---

## Anthropic's Official Workflow

### Best Practices статья

Anthropic має офіційну стать: [claude-code-best-practices](https://www.anthropic.com/engineering/claude-code-best-practices)

Там рекомендують:

1. **Planning** - використовуйте /think, /think hard, /ultrathink
2. **Exploring** - використовуйте sub-agents для дослідження
3. **Coding** - виконуйте з хорошої комунікацією
4. **Testing** - запускайте тести відразу

![Anthropic Best Practices](./screenshots/best-practices.webp)

**Ключевые советы:**
- Когда использовать Plan mode
- Когда использовать sub-agents  
- Какие промпты (/think vs /ultrathink)
- Как организовать feedback

---

## Практичні совіти

### Совіт 1: Вивчи скорочення першим

```
Shift + ? → список скорочень
Запам'ятай:
- ! для bash
- @ для файлів
- / для команд
- Esc+Esc для форкування
```

### Совіт 2: Не копіюй контекст вручну

```
❌ Копіюй-паста з chat
✅ Використовуй @ або попроси Claude писати у файли
```

### Совіт 3: Читай Anthropic's Best Practices

Це коротко але компактно.

### Совіт 4: Експериментуй з командами

Як тільки зрозумієш основи, почни створювати свої команди!

---

## Список корисних команд

| Команда | Функція | Потребує |
|---------|---------|----------|
| `!` | Bash коман | - |
| `@file` | Посилання на файл | - |
| `/` | Список команд | - |
| `/review` | Код огляд | GitHub CLI |
| `/pr-comments` | PR коментарі | GitHub CLI |
| `/think` | Звичайне мислення | - |
| `/ultrathink` | Глибоке мислення | - |
| Esc+Esc | Форкування | - |
| `Shift+?` | Скорочення | - |

---

## Висновок розділу

Команди та совіти - це **не просто nice-to-have**, це **force multipliers**:

1. **Скорочення** ускорюють вас (`!`, `@`)
2. **Команди** автоматизують повторювані завдання (`/review`)
3. **Memory** зберігають ваші преференції (глобальні)
4. **CLAUDE.md** організує проектні інструкції (локальні)

**Комбінація** цих інструментів робить вас набагато продуктивнішим! 🚀

---

**Далі**: [Майбутні плани →](./06-future.md)
