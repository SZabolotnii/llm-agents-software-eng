# 6. Глибокий огляд функцій

Далі йдуть розділи про найбільш використовуються функції.

---

## Команди (Commands)

Вам можна не охопити команди в моєму попередньому посібнику. Давайте докладно розберемо.

### Що таке команди?

Ви можете використовувати **`/`** для доступу до вбудованих команд. Це **попередньо визначені промпти**, які виконують конкретне завдання.

**Приклади вбудованих команд:**
- `/clear` - очистити контекст і почати заново
- `/compact` - стиснути розмову (швидко в CC 2.0)
- `/review` - запросити огляд коду
- `/resume` - поновити розмову

### Користувальницькі команди

Якщо вбудовані команди не охоплюють те, що вам потрібно, ви можете **створити власні команди**.

**Дві локації для команд:**
- **Проектна рівень**: `.claude/commands/` - діє тільки для цього проекту
- **Глобальна рівень**: `~/.claude/commands` - діє для всіх проектів

### Як створити команду

**Ви можете просто сказати Claude**: "Створи користувальницьку команду для [завдання]"

Claude знає, як це робити (або пошукає в web та помучиться).

![Створення користувальницької команди](https://bear-images.sfo2.cdn.digitaloceanspaces.com/sankalp/17am.webp)

### Мій приклад: `/handoff` команда

Часто, коли контекстне вікно переповнюється або модель борсть з складним завданням, я хочу почати нову розмову.

Вбудована `/compact` швидка, але іноді я віддаю перевагу, щоб Claude **записала, що сталося в поточній сесії** (з деякою специфічною інформацією) перш ніж я завершу розмову і почну нову.

Я створив `/handoff` команду для цього.

![Bootstrap repo](https://bear-images.sfo2.cdn.digitaloceanspaces.com/sankalp/49am.webp)

Дивіться "Explore" суб-агент та "running in background" статус.

---

## Суб-агенти (Sub-agents)

Суб-агенти були введені невдовзі після моєї останньої статті. Вони - **окремі інстанції Claude**, породжені основним агентом на його власне судження або коли ви йому це скажете.

### Що таке суб-агент?

**Суб-агент** - це окремий екземпляр агента, який:
- 📝 Має свою власну систему промпту
- 🎯 Спеціалізується на конкретному завданні
- 🔄 Запускається окремо від основного агента
- 📤 Повертає результати до основного агента

### Типи суб-агентів

#### 1. **General-purpose** 🚀
- Універсальний агент для дослідження складних питань
- Може шукати код та виконувати багатокрокові завдання
- **Повна доступ до всіх інструментів**
- Успадковує повний контекст від основного агента

#### 2. **Explore** 🔍
Спеціаліст з пошуку файлів для навігації по кодовим базам.

**Основні особливості:**
- Може використовувати Glob, Grep, Read, обмежені Bash-команди
- **СТРОГО заборонено** створювати або модифікувати файли
- Чудова для швидкого пошуку файлів за патернами
- Чудова для пошуку коду за ключовими словами

**Коли використовувати:**
```
"Launch explore agent - find all React components"
"Explore agent - search for API endpoints"
"Explore agent - answer how authentication works"
```

**Рівні детальності:**
- `"quick"` - базовий пошук
- `"medium"` - помірна розробка
- `"very thorough"` - комплексний аналіз

#### 3. **Plan** 📐
Агент-архітектор для планування стратегії реалізації.

**Використовується для:**
- Планування розробки
- Ідентифікація критичних файлів
- Розгляд архітектурних компромісів

#### 4. **claude-code-guide** 📚
Спеціаліст по документації Claude Code.

**Використовується для запитань про:**
- Claude Code (CLI) - функції, команди, інтеграції
- Claude Agent SDK - розробка користувальницьких агентів
- Claude API - використання API, tool use, SDK

---

## Суб-агенти наслідує контекст?

**Ключова відмінність:**

- ✅ `general-purpose` та `Plan` - **успадковують повний контекст**
- ❌ `Explore` - **стартує з чистого листа** (чому? пошукові завдання часто незалежні)

### Чому це важливо?

Якщо я шукаю щось в кодовій базі, попередня розмова може не бути релевантною. Давайте Explore почнеться свіжим.

**Але є вловка**: Explore повертає **резюме** до основного агента.

> **Критично**: Модель мусить прочитати кожен релевантний файл сама по собі, щоб весь контекст міг взаємодіяти.

Це важливо, оскільки:
1. **Резюме** можуть **втратити інформацію** (lossy compression)
2. Коли Opus 4.5 читає кожен файл, він знає, **які деталі релевантні до якого контексту**
3. Це дозволяє моделі витягти більше **попарних взаємозв'язків**

**Аналогія**: Замість того, щоб дати кому-то резюме дослідження, краще дати їм всі папери для читання - вони зможуть скласти кращішу картину.

---

## Task Tool - Як народжуються суб-агенти

Суб-агенти запускаються через **Task tool** на основі змішаного підуму основного агента.

### Як дізнатися про Task tool?

**Ви можете просто запитати Claude:**

```
1. "Tell me the Task tool description"
2. "Give me full description"  
3. "Show me entire tool schema"
```

Це **не галюцинація** - це справжній інструмент! Розробники дозволяють це.

### Task Tool Schema

Ось спрощена версія:

```json
{
  "description": "Launch a new agent to handle complex tasks",
  "properties": {
    "description": "A short (3-5 word) description",
    "prompt": "The task for the agent",
    "subagent_type": "general-purpose | Explore | Plan | claude-code-guide",
    "model": "sonnet | opus | haiku (optional, defaults to parent)",
    "run_in_background": "true | false (optional)"
  }
}
```

**Ключ**: Параметр `model` дозволяє вам сказати "Запустити Explore з Sonnet" замість Haiku (за замовчуванням).

### Background Agents (корисно для налагодження!)

Параметр `run_in_background` дозволяє інструменту бігти у фоні, mientras ви робите щось інше.

**Випадки використання:**
- 🖥️ Довготривалий скрипт Python
- 📋 Моніторинг логів процесу
- 🔄 Проходження серії тестів

---

## Практичні приклади

### Приклад 1: Швидкий пошук в кодовій базі

```
User: "Find all authentication-related code"

Claude:
1. Spawns: Explore agent (quick)
2. Result: "Found 5 files with auth logic"
3. Then reads all 5 files itself
4. Explains how auth works in this codebase
```

### Приклад 2: Планування нової функції

```
User: "Plan how to add dark mode"

Claude:
1. Spawns: Plan agent
2. Result: Step-by-step implementation plan
3. Identifies critical files to modify
4. Considers design trade-offs
5. You decide to proceed or adjust
```

---

## Команди vs Sub-агенти

| Аспект | Команди | Sub-агенти |
|--------|---------|-----------|
| Мета | Виконати специфічну задачу з текстом | Запустити окремий LLM для складного завдання |
| Контекст | Додається до поточної розмови | Може успадкувати (general) або почати свіжим (Explore) |
| Спеціалізація | Загальні промпти | Спеціалізовані для різних завдань |
| Коли використовувати | Повторювані завдання, статичні інструкції | Складні дослідження, багатокрокові процеси |

---

**Далі**: [Мій робочий процес →](./07-workflow.md)
