# 8. Контекстна інженерія

Це серце розуміння того, як робить Claude Code "магія". Давайте розберемо, як області контексту та як це оптимізувати.

---

## Агенти - машини для гузання токенів

Агенти можуть проактивно:
- 📖 Читати файли
- 🔧 Редагувати речі
- 🔍 Шукати файли
- 📊 Аналізувати дані

На кожному кроці вони **генерують багато даних**, які додаються до контекстного вікна.

### Приклад: Складання посадкової сторінки

```
Контекстне вікно:
├─ User: "Зробіть посадкову сторінку для моєї кав'ярні"
│
├─ Claude: [tool_call: web_search("сучасний дизайн посадкової сторінки")]
├─ Результат інструменту: [10 результатів] ← ~1.5K токенів
│
├─ Claude: [tool_call: read_file("brand-guidelines.pdf")]
├─ Результат інструменту: [витяг текст, кольори, шрифти] ← ~4K токенів
│
├─ Claude: "Я створю теплий дизайн..."
├─ Claude: [tool_call: create_file("landing.html")]
├─ Результат інструменту: [успіх, 140 рядків] ← ~50 токенів
│
├─ Claude: [tool_call: edit_file("landing.html")]
├─ Результат інструменту: [diff: додано герой + меню] ← ~300 токенів
│
└─ Claude: "Готово!"

Всього: ~6K+ токенів для одного завдання!
```

### Ключова точка: LLM не мають пам'яті

```python
# Це як це працює:

# Повідомлення 1
user_input = "Make a landing page"
context = [original_system_prompt, user_input]
response = model.generate(context)  # ← Читає контекст

# Повідомлення 2
user_input2 = "Add dark mode"
# ВАЖЛИВО: Контекст має все попереднє!
context = [original_system_prompt, message_1, response_1, user_input2]
response2 = model.generate(context)  # ← Читає весь старий + новий контекст
```

**Це означає**: Якщо ви не додасте інформацію про те, яка функція була викликана, модель не матиме про це інформації.

**Це також означає**: Результати інструменту **кількома заповнять ваш контекст** дуже швидко.

---

## Контекстна інженерія

Цитата прямо з [Anthropic про контекстну інженерію](https://www.anthropic.com/engineering/effective-context-engineering-for-ai-agents):

> **Контекстна інженерія** - це про відповідь на питання: **яка конфігурація контексту найімовірніше призведе до бажаної поведінки нашої моделі?**

Все, про що ми говорили до цього, входить в контекстну інженерію:
- Суб-агенти
- Скетчпад
- Компактація
- Команди

---

## Деградація контексту (Context Rot)

### Проблема: Контекстне вікно не безмежне

Когда ви додаєте все новий токен у контекст, **ефективність контекстного вікна деградує**.

Це через **механізм уваги** (attention mechanism) - стає важче **моделювати попарні взаємозв'язки** між токенами, які далеко один від одного.

**Аналогія**: Ваш стіл із робочою площею. Чим більше паперів на столі, тим складніше фокусуватися на потрібному папері.

### Дослідження контекстної деградації

[Chroma's Context Rot Research](https://research.trychroma.com/context-rot) показує:

```
Performance vs Context Length

Performance ↑
    |     ████████████
    |     ████████░░░░░░░░░
    |     ████░░░░░░░░░░░░░░░░░
    |     ░░░░░░░░░░░░░░░░░░░░░░░░░░
    |___________________________________→ Context Length
```

**Висновок**: Ефективні контекстні вікна - це **50-60% або навіть менше** від оголошеної довжини!

### Практичні наслідки

- Opus 4.5 має **200K** контекстного вікна
- Но ефективна робота, вважайте її **100K-120K токенів**

**Поради**:
1. ❌ Не починайте складне завдання на 50% контексту
2. ✅ Використовуйте `/context` для монітор
3. ✅ Робіть `/compact` або `/handoff` при 60%
4. ✅ Починайте нову розмову для складних завдань

---

## Як контекстна інженерія працює в Claude Code

Усе в Claude Code спрямоване на оптимізацію контексту:

### 1. Підключення релевантного контексту

```
╔════════════════════════════════════╗
║   Що повинно бути у контексті?    ║
╠════════════════════════════════════╣
║ ✅ Поточна задача                  ║
║ ✅ Релевантні файли                ║
║ ✅ Останні результати інструментів ║
║ ❌ Забуті розмови з минулого       ║
║ ❌ Непотрібні деталі               ║
║ ❌ Дублювання інформації           ║
╚════════════════════════════════════╝
```

### 2. Скорочення бульк-інформації (Context Bloat Reduction)

**Методи:**
- 🗂️ **Суб-агенти для пошуку** - Explore агент повертає резюме, не повний файл
- 📋 **Скетчпад** - записувати важні висновки отдельно
- 🗜️ **Компактація** - стискає розмову
- 🔄 **Handoff** - передавання контексту в нову розмову через резюме

### 3. Чітка структура інструкцій

Система промптів мусить мати:
- 🎯 **Чіткі цілі** - що робити
- 🚫 **Чіткі обмеження** - чого не робити
- 🔀 **Мало конфліктуючих інструкцій** - легше для моделі дотримуватися

### 4. System-Reminders

Одна тактика для боротьби з контекстною деградацією - **повторно впровадж цілі в контекст**.

**Як це працює:**

```python
# Замість просто один раз сказати мету...
instructions = "Build a login system"

# ...claude Code повторно додає нагадування:
[tool_result]
<system-reminder>
Remember: Your goal is to build a login system with:
- Email/password auth
- Session management
- Rate limiting
</system-reminder>
```

[Manus описує це краще](https://manus.im/blog/Context-Engineering-for-AI-Agents-Lessons-from-Building-Manus):

> "Постійно переписуючи список TODO, Manus постійно **повторно впровадимає цілі в кінець контексту**. 
> Це **штовхає глобальний план в недавну увагу моделі**, уникаючи "lost-in-the-middle" проблем."

---

## MCP сервери і виконання коду

### Проблема з MCP

MCP сервери - це чудові для інтеграцій, але:

**Визначення інструментів завантажуються на початку в контекст**, що **розбухує контекст**.

### Рішення: Code Execution з MCP

Замість численних визначень інструментів, Anthropic пропонує:

```
❌ Визначити 50 інструментів у промпті
✅ Дати Claude простір для виконання коду + файлова система
✅ Claude пише код для виклику інструментів
✅ Код виконується у пісочниці
✅ Результати повертаються
```

Це **більш елегантно** і витрачає менше токенів на визначення!

---

## Навички (Skills)

Навички - це **на-попит завантаження знання**.

**Замість**: Писати все знання в системний промпт  
**Розробніки**: Упакувати знання в папку з SKILL.md, Claude завантажує, коли необхідно

**Як це працює:**

```
Claude sees task → "I need Kubernetes knowledge" 
                 → Loads /skills/kubernetes/SKILL.md 
                 → Claude now has the knowledge
                 → Executes task
```

Це **ефективна контекстна інженерія** - завантаження на потребу замість завжди-присутнього.

---

## Гаки (Hooks)

Гаки дозволяють спостерігати за етапами в агентському циклі і запускати bash-скрипти.

**Приклад гака для напередачення цілі:**

```bash
# hooks/before-tool-call.sh
# Запускається перед кожним функціональним викликом

echo "Remember: Your goal is to build X"
echo "Current progress: $(cat progress.txt)"
```

Це **вручну впровадає напередачення цілі**, що допомагає боротися з контекстною деградацією.

---

## Комбінування: Гаки + Навички + Нагадування

Давайте поєднаємо все разом:

![Комбінування функцій](https://bear-images.sfo2.cdn.digitaloceanspaces.com/sankalp/11am.webp)

**Приклад стратегії:**

```
1. Напишіть CLAUDE.md з основними інструкціями
2. Розбийте великі знання на Skills (~500 рядків кожний)
3. Створіть Hook для напередачення цілі кожні N кроків
4. Использовуйте System Reminders для критичних обмежень
5. Монітор контексту через /context
6. Компактуйте при 60% використання
```

Це **систематичний підхід** до керування контекстом!

---

## Висновок

Контекстна інженерія - це **мистецтво та наука** про оптимізацію обмеженого контекстного вікна.

**Запам'ятайте:**
- 🎯 Контекст - це обмежена ресурс
- 📉 Деградація контексту реальна
- 🛠️ Claude Code має інструменти для боротьби з цим
- 🧩 Комбінуйте підходи для найкращих результатів

---

**Далі**: [Висновки та рекомендації →](./09-conclusion.md)
